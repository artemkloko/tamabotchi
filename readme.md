# artemkloko/tamabotchi

`tamabotchi` is just a funny bot based on Markov Chains.

## Usage

- `npm install`
- `cp config.sample.json config.json`
- edit `config.json` to your liking
- `node index.js`
